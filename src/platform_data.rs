mod platform_data {
    pub struct PlatformData {
        sdk_type: str,
        sdk_version: str,
        platform_version: str,
        device_model: str,
        platform: str,
        hostname: str,
    }
}